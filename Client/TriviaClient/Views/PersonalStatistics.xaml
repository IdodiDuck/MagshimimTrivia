<Page x:Class="TriviaClient.PersonalStatistics"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Personal Statistics">

    <Grid Background="#FF1E1E1E">
        <!-- Main Panel -->
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Width="636"
                    Margin="0,28,0,0"
                    Orientation="Vertical" Height="341">
            <!-- Headers -->
            <TextBlock Text="Personal Statistics"
                       FontSize="24"
                       Foreground="#FFCCCCCC"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       Margin="0,10,0,20"
                       HorizontalAlignment="Center"/>

            <!-- Statistics Headers & Data -->
            <Grid Margin="0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="42*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Games Played:" Foreground="#FFCCCCCC" FontWeight="SemiBold" FontSize="20" Grid.Column="0"/>
                <TextBlock x:Name="gamesPlayed"  Foreground="#FFCCCCCC" Text="{Binding Path=GamesPlayed, UpdateSourceTrigger=PropertyChanged}" FontSize="20" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

            <Grid Margin="0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Correct Answers:" Foreground="#FFCCCCCC" FontWeight="SemiBold" FontSize="20" Grid.Column="0"/>
                <TextBlock x:Name="correctAns" Foreground="#FFCCCCCC" Text="{Binding Path=CorrectAnswers, UpdateSourceTrigger=PropertyChanged}" FontSize="20" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

            <Grid Margin="0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Wrong Answers:" Foreground="#FFCCCCCC" FontWeight="SemiBold" FontSize="20" Grid.Column="0"/>
                <TextBlock x:Name="wrongAns" Foreground="#FFCCCCCC" Text="{Binding Path=WrongAnswers, UpdateSourceTrigger=PropertyChanged}" FontSize="20" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

            <Grid Margin="0,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Average Answer Time:" Foreground="#FFCCCCCC" FontWeight="SemiBold" FontSize="20" Grid.Column="0"/>
                <TextBlock x:Name="avgAnsTime" Foreground="#FFCCCCCC" Text="{Binding Path=AverageAnswerTime, UpdateSourceTrigger=PropertyChanged}" FontSize="20" Grid.Column="1" Margin="10,0,0,0"/>
            </Grid>

        </StackPanel>

        <!-- Buttons -->
        <Button Content="Back to Menu"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Width="150"
                Height="40"
                Margin="0,0,20,20"
                Background="White" Foreground="Black"
                Click="BackToMenu_Click"/>

    </Grid>
</Page>
