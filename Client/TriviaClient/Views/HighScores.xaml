<Window x:Class="TriviaClient.HighScores"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Top Players" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Text="Best Scores" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,10,0,20"/>

        <!-- First Player -->
        <TextBlock Text="1. Player1 - Score: 0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,10"/>

        <!-- Second Player -->
        <TextBlock Text="2. Player2 - Score: 0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,10"/>

        <!-- Third Player -->
        <TextBlock Text="3. Player3 - Score: 0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,10"/>

        <!-- Button -->
        <Button Content="Back to Menu"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Width="150"
                Height="40"
                Margin="0,0,20,20"
                Background="White"
                Foreground="Black"
                Grid.Row="4" Grid.Column="2"
                Click="BackToMenu_Click"/>
    </Grid>
</Window>
