<Window x:Class="TriviaClient.Signup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TriviaClient"
        mc:Ignorable="d"
        Title="Signup" Height="500" Width="800" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Background="#E5E5E5E5">
        <!-- Main content container -->
        <Border CornerRadius="10" 
                Background="#FBFBFBFB" 
                Width="350" Height="450"
                VerticalAlignment="Center" HorizontalAlignment="Center">

            <StackPanel Margin="20" VerticalAlignment="Center">
                <!-- Header with logo -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,3">
                    <TextBlock Text="Sign Up" 
                               Foreground="Black"
                               FontSize="30" FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Username field -->
                <TextBlock Text="Username" Foreground="#EE101010" Margin="5,0"/>
                <TextBox x:Name="UsernameTextBox" 
                         Height="40" Margin="0,5,0,5"
                         Padding="10"
                         Background="#20202020"
                         Foreground="Black"
                         BorderBrush="#3A3A3A3A"
                         BorderThickness="2"
                         CaretBrush="Black"/>

                <!-- Email field -->
                <TextBlock Text="Email" Foreground="#EE101010" Margin="5,0"/>
                <TextBox x:Name="EmailTextBox" 
                         Height="40" Margin="0,5,0,5"
                         Padding="10"
                         Background="#20202020"
                         Foreground="Black"
                         BorderBrush="#3A3A3A3A"
                         BorderThickness="2"
                         CaretBrush="Black"/>

                <!-- Password field -->
                <TextBlock Text="Password" Foreground="#EE101010" Margin="5,0"/>
                <PasswordBox x:Name="PasswordBox" 
                             Height="40" Margin="0,5,0,5"
                             Padding="10"
                             Background="#20202020"
                             Foreground="Black"
                             BorderBrush="#3A3A3A3A"
                             BorderThickness="2"/>
                
                <!-- Sign up button -->
                <Button x:Name="SignUpButton" 
                         Content="Sign Up"
                         Height="40" Margin="0"
                         Background="#EAEAEAEA"
                         Foreground="Black"
                         FontWeight="Bold"
                         BorderThickness="0"
                         Cursor="Hand"
                         IsEnabled="False"
                         Click="SignUpButton_Click" Width="310"/>

                <!-- Back to login link -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Already have an account? " Foreground="#FFA0A0A0"/>
                    <TextBlock Text="Login" 
                               Foreground="#FF007ACC"
                               Cursor="Hand"
                               MouseLeftButtonUp="Login_MouseLeftButtonUp">
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>